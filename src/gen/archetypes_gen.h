// AUTO-GENERATED by train/gen_archetypes.py — do not edit manually.
// Inputs: train/archetypes.py (ARCHETYPES order), bin/resources/decks/archetypes.json
// C++ mirror of the deck -> archetype -> value-bucket metadata, so the
// in-process AZ actor's observation tail (src/actor/obs_builder.cpp) is
// identical to the one train/env.py appends. Keys are already normalized
// the way archetypes.normalize_deck() normalizes a deck spec: lowercase,
// decks/-relative, no '.dk' suffix, '/' separators.
#pragma once

// Archetype slots: the tagged archetypes in ARCHETYPES order, then the
// 'unknown' fallback bucket at index ARCH_UNKNOWN.
static const int ARCH_N = 8;
static const int ARCH_UNKNOWN = 7;
// Value buckets span (self archetype x opponent archetype).
static const int ARCH_N_VALUE_BUCKETS = 64;

static const char* const ARCH_NAMES[8] = {
    "tempo",  // 0
    "slow_midrange",  // 1
    "control",  // 2
    "burn",  // 3
    "lands",  // 4
    "doomsday",  // 5
    "reanimator",  // 6
    "unknown",  // 7
};

struct ArchDeckTag {
    const char* stem;  // normalized decks/-relative stem
    int arch;          // index into ARCH_NAMES
};

static const int ARCH_N_DECK_TAGS = 13;
static const ArchDeckTag ARCH_DECK_TAGS[13] = {
    {"delver", 0},  // tempo
    {"doomsday", 5},  // doomsday
    {"league/bug", 1},  // slow_midrange
    {"league/burn", 3},  // burn
    {"league/bw_dnt", 1},  // slow_midrange
    {"league/gw_maverick", 0},  // tempo
    {"league/lands", 4},  // lands
    {"league/reanimator", 6},  // reanimator
    {"league/ur_delver", 0},  // tempo
    {"league/uw_control", 2},  // control
    {"league/wrb_energy", 0},  // tempo
    {"league/wubg_doomsday", 5},  // doomsday
    {"mav", 0},  // tempo
};

